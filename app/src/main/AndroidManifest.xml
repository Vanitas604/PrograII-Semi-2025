<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal"
        tools:targetApi="31">

        <!-- Lista de actividades -->
        <activity android:name=".AgregarTarea" />
        <activity android:name=".Lista_Grupos" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CrearCuenta" />
        <activity android:name=".TareasPorGrupo" />
        <activity android:name=".TareasPendientes" />
        <activity android:name=".TareasRealizadas" />
        <activity android:name=".AjustesActivity" />
        <activity android:name=".GraficaActivity" />
        <activity android:name=".ChatUsuariosActivity" />
        <activity android:name=".AgregarContactoActivity" />
        <activity android:name=".ListaContactosActivity" />
        <activity android:name=".Chats" />



        <!-- LoginActivity como pantalla inicial -->
        <activity android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receptor de notificaciones (recordatorios automáticos) -->
        <receiver android:name=".ReminderReceiver" android:exported="false" />
        <receiver android:name=".Notificador" android:exported="false" />

    </application>

</manifest>